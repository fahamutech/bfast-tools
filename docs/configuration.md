# Configuration

`bfast-tools` is now local-only and does not require cloud login, project linking, or remote API configuration.

## Runtime environment values (local serve)

`bfast fs serve` sets or uses:
- `PRODUCTION`
- `DEV_WORK_DIR`
- `DEV_PORT`
- `IS_LOCAL_BFAST=true`
